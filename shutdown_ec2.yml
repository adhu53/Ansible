---
 - name: To shutdown the EC2 machine for list of hosts1 in group1
   hosts: group1
   become: yes
   gather_facts: true
   tasks:
     - name: Shut down the machine if it is Ubuntu machine after 30 seconds.
       shell: /sbin/shutdown now
       when: ansible_distribution=='Ubuntu'
